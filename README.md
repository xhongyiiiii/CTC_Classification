# CTC Classification System

This project provides a **Streamlit-based UI system** for circulating tumor cell (CTC) classification using a deep learning model.  
Users can upload images through the UI and obtain classification results generated by the trained model.

---

# Project Structure

project/
│
├── UI/
│ └── UI.py # Streamlit user interface
│
├── models/
│ └── ... # Model architecture source code
│
├── weights/
│ └── best_model.pth # Trained model weights
│
├── requirements.txt # Python environment dependencies
│
└── README.md


---

# Environment Setup

It is recommended to create a Python virtual environment before installing dependencies.

### Install dependencies

```bash
pip install -r requirements.txt

Run the User Interface
Navigate to the UI directory and run the Streamlit application.
streamlit run UI.py

weights
Contains the trained model weights.
weights/best_model.pth
